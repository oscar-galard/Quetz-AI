"""Port contracts (interfaces) implemented by infrastructure adapters.

Ports are protocol classes. Application-level use cases depend only on these
abstractions (Dependency Inversion), never on LangChain or concrete adapters.
"""
