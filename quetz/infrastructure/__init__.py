"""Infrastructure layer: adapters, graph, and observability.

This layer implements the application ports and owns all framework and tool
integration (LangChain, LangGraph, LangSmith), keeping the outer layers pure.
"""
